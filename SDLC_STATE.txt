=== 01. STATE METADATA ===
SCHEMA VERSION: 1
PROJECT: pongoOS
REPOSITORY ROOT: /Users/kannankhosla/Desktop/Codex/pongoOS
LAST UPDATED: 2026-08-10T07:14:11Z
EXECUTION MODE: Release Readiness and Release
CURRENT PHASE: Full-file stock override deployed and verified on Heroku v43
CURRENT RELEASE RECOMMENDATION: RELEASED AND VERIFIED

=== 02. EXECUTIVE STATUS IN PLAIN LANGUAGE ===
Heroku v43 is live with direct full-inventory CSV stock replacement. Only SKU and In stock are mandatory, safe blank locations resolve automatically, matching quantities are skipped, and any bad/excluded/stale row prevents every stock change. A generated 1,500-row file committed as one adjustment and one transaction in the local regression test; production was verified without uploading a CSV or changing stock.

=== 03. PRODUCT VISION, USERS, AND SUCCESS METRICS ===
Primary users are Pongo warehouse staff and delivery coordinators using desktop, iPhone, or Android. Success means a scanned code immediately runs the current Items/Inventory search, and every routable open order appears exactly once in a visible driver plan. Staff can change driver count, optionally return to the warehouse, and share working Google Maps links without configuring map credentials.
Stock CSV success means staff can export/edit a CSV, preview exact per-location quantity changes, and commit valid rows without manually adjusting each item; every changed quantity remains auditable and allocated stock remains system-controlled.

=== 04. REQUIREMENTS AND ACCEPTANCE CRITERIA ===
REQ-SCAN-001: Items and every Inventory subpage expose the existing rear-camera QR/barcode scanner with manual fallback.
REQ-SCAN-002: A decoded or manually entered scanner value immediately executes the current page search and does not mutate stock.
REQ-ROUTE-001: Planner defaults to 5855 99 Street NW, Edmonton, AB and accepts 1–50 drivers plus optional return-to-start.
REQ-ROUTE-002: Every non-historical operational open order with a complete address is assigned to exactly one driver; incomplete addresses are explicitly listed.
REQ-ROUTE-003: Assignments keep postal areas together where possible and balance stop counts deterministically.
REQ-ROUTE-004: Every driver receives shareable Google Maps driving links usable on mobile; long routes are continuous numbered parts with no more than three intermediate waypoints.
REQ-ROUTE-005: Planning is read-only and does not persist routes or mutate orders, WooCommerce, inventory, or stock audit data.
REQ-QA-001: Focused and full backend/frontend tests, frontend build, diff checks, documentation, and available browser verification pass before deployment.
REQ-STOCKCSV-001: The item import workspace exposes an Override stock levels outcome with a schema-owned CSV template.
REQ-STOCKCSV-002: Each row identifies an existing item by exact SKU and a non-negative exact In stock quantity; warehouse/location optionally narrow an existing active assignment and zero is valid.
REQ-STOCKCSV-003: Preview displays current quantity, requested quantity, and variance without mutating stock. Invalid, duplicate, unmatched, or stale rows are not committed.
REQ-STOCKCSV-004: Commit applies the complete ready file as one idempotent audited stock adjustment, recalculates item totals through the existing stock service, and does not accept Allocated or Sellable overrides.
REQ-STOCKCSV-005: Retrying the same commit cannot apply stock twice, and a quantity changed after preview must require re-preview rather than silently overwrite newer work.
REQ-STOCKCSV-006: A full inventory export with extra columns may be uploaded directly; SKU and In stock are the only mandatory stock fields, while warehouse/location narrow the target when supplied.
REQ-STOCKCSV-007: A stock import is all-or-nothing. Unmatched, ambiguous, invalid, stale, or excluded rows block the entire commit; unchanged rows are skipped.
REQ-STOCKCSV-008: All changed rows commit in one database transaction and one audited adjustment, including a 1,500-product file; the UI must not imply partial batches.

=== 05. SCOPE, NON-GOALS, CONSTRAINTS, AND ASSUMPTIONS ===
IN SCOPE: phone camera scanner entry points on Items/Inventory; open-order query and deterministic driver assignment; missing-address reporting; keyless Google Maps URL construction; native share/clipboard fallback; responsive UI; tests and docs.
NON-GOALS: configuration changes, embedded map tiles, paid Google Routes API, traffic-aware optimization, server-side geocoding/address correction, route persistence, dispatch tracking, driver accounts, proof of delivery, WooCommerce writes, and stock changes.
CONSTRAINTS: Frontend receives no map credentials. Google Maps URLs have mobile waypoint limits, so long routes must be segmented. Existing completed-order route records remain available as a separate historical workflow.
ASSUMPTION: Operational open orders are the existing non-historical Woo processing/FooSales set and shipping address fields are the delivery authority.
STOCK CSV IN SCOPE: CSV-only exact per-location stock overrides through the existing six-step item import workspace, validation/preview, audit movements, history, tests, and docs.
STOCK CSV NON-GOALS: changing allocated/sellable quantities, creating items/warehouses/locations, XLSX support, background processing, or metadata rollback for stock.
CURRENT STOCK CSV INPUT FACT: The user's inventoryexport.csv has 1,726 data rows, one warehouse, 71 named locations, 456 blank location cells, no blank SKUs or stock values, no negative stock, and no duplicate SKU/warehouse/location scopes. Extra columns are intentionally ignored.

=== 06. SYSTEM, JOURNEY, AND DATA MAP ===
Scanner journey: Items or Inventory -> Scan code -> rear camera or manual code -> existing local search -> matching item results.
Route journey: Routes -> default warehouse/start + driver count -> POST /api/routes/open-orders/plan -> one read-only operational order query -> address validation -> postal-area sort -> balanced driver groups -> continuous Google Maps URLs -> open/share.
Data boundaries: Order/customer/address snapshots travel from authenticated Pongo backend to the authenticated frontend. They are not sent to Google by the backend; Google receives addresses only when staff opens or shares a generated URL.
Stock CSV journey: Items -> Import CSV -> Override stock levels -> upload/map columns -> review current/new/variance -> confirm -> one audited stock adjustment -> results/history. Quantity authority is the selected active inventory location; item totals remain derived by the existing location inventory service.

=== 07. ARCHITECTURE AND DECISION REGISTER ===
ADR-036 ACCEPTED: Use keyless Google Maps URLs, not a map SDK or paid routing API.
ADR-036A ACCEPTED: Deterministic postal-area sorting plus balanced contiguous groups provides predictable assignment without claiming traffic optimization.
ADR-036B ACCEPTED: Limit each mobile delivery link to four stops (three intermediate waypoints plus destination); start later parts at the previous destination.
ADR-036C ACCEPTED: Keep this planner read-only and separate from saved completed-order route records.
ADR-SCAN-001 ACCEPTED: Reuse MobileCodeScanner and the page's existing search pipeline instead of creating a second scanner/search backend.
ADR-STOCKCSV-001 ACCEPTED: Reuse the item import workspace for mapping/preview/idempotency and the stock adjustment service for locks, totals, and audit movements; CSV In stock is an absolute location quantity, not a delta.
ADR-STOCKCSV-002 ACCEPTED: SKU is the item identity; Warehouse and Inventory location narrow stock scope when present, while blank scope is resolved only when unambiguous or already matching. Allocated/Sellable remain derived operational fields.

=== 08. ROADMAP AND ACTIVE DELIVERY PLAN ===
1. Inspect scanner, order, route, and UI architecture. COMPLETED.
2. Add read-only open-order route planning schemas/service/API. COMPLETED.
3. Add driver planning, stop review, Google Maps open/share, exclusions, and responsive UI. COMPLETED.
4. Add camera scanner to every Inventory subpage and make scan results search immediately. COMPLETED.
5. Add focused backend/frontend coverage and update product/API/QA/decision docs. COMPLETED.
6. Run full regression, build, state validation, and final browser cleanup. COMPLETED.
7. Back up PostgreSQL, deploy to Heroku, restore the worker, and verify production. COMPLETED.
8. Define stock CSV contract and update lifecycle state. COMPLETED.
9. Implement backend schema, preview validation, locked audited commit, and API results. COMPLETED.
10. Expose the outcome in the existing import UI and add regression coverage/docs. COMPLETED.
11. Run focused/full verification and hand off without deployment. COMPLETED.
12. Back up PostgreSQL, deploy the verified stock CSV release, restore the worker, and verify production without changing inventory. COMPLETED.
13. Repair direct full-export compatibility and enforce an atomic all-or-nothing stock commit. COMPLETED.
14. Verify generated 1,500-row behavior, deploy with a new backup, and recheck production without changing stock. COMPLETED.

=== 09. WORK ITEM REGISTER ===
SCAN-001 / P0 / COMPLETED / Reused phone-camera scanner across Items and Inventory; detected values execute search immediately.
ROUTE-001 / P0 / COMPLETED / Read-only all-open-order route planning API with address exclusions and 1–50 driver balancing.
ROUTE-002 / P0 / COMPLETED / Mobile-safe continuous Google Maps direction URLs with optional warehouse return.
ROUTE-003 / P0 / COMPLETED / Polished responsive planner, per-driver stops, warnings, native share/clipboard, and Google Maps actions.
QA-001 / P0 / COMPLETED / Full backend/frontend regression, build, diff check, desktop/mobile browser layout, and state validation passed.
RELEASE-001 / P1 / RELEASED / Heroku v40 deployed commit 96c3816 after manual backup b004; web/worker, migration, health, authentication, Routes, Inventory scanner, and browser logs verified.
WI-STOCKCSV-001 / P0 / RELEASED / Exact per-location stock override through item import CSV, using audited stock adjustment primitives and stale-preview protection; deployed and verified on production as Heroku v42.
WI-STOCKCSV-002 / P0 / RELEASED / Full inventory export uploads directly; unchanged rows skip, safe blank locations resolve, exclusions/errors block the file, and changed rows commit in one locked audited transaction; deployed as Heroku v43.

=== 10. RISK, DEPENDENCY, AND BLOCKER REGISTER ===
RISK-001 / MEDIUM / MITIGATED: Google mobile browser links accept fewer waypoints. Each part uses at most three intermediate waypoints and continues from the prior destination.
RISK-002 / MEDIUM / ACCEPTED: Postal grouping is not road/traffic optimization. UI and docs state this clearly; Google Maps provides directions only after the link opens.
RISK-003 / MEDIUM / MITIGATED: Incomplete addresses could silently disappear. Planner returns and displays every excluded order and reason.
RISK-004 / LOW / MITIGATED: More drivers than orders. Effective driver count is reduced and the response explains why.
RISK-005 / MEDIUM / MITIGATED FOR RELEASE: The obsolete local SQLite file was not used for release evidence. Heroku's PostgreSQL release migration succeeded at revision 20260807_0037 and the authenticated production UI loaded real route/inventory data.
RISK-006 / HIGH / PRE-EXISTING AND ACCEPTED FOR THIS RELEASE: Production `/ready` remains HTTP 503 because 223 active stock items lack positive unit cost, no operations alert webhook is configured, and production stock authority/writeback are not fully configured. This delivery did not alter those settings; `/health`, schema, authentication, database, webhooks, reconciliation, and worker checks passed.
DEPENDENCY-001: Opening a generated link requires Google Maps/web and exposes that link's addresses to Google at staff action time.
RISK-STOCKCSV-001 / HIGH / MITIGATED: Preview performs a stale check and the adjustment service repeats the expected-quantity check under the stock lock before writing.
RISK-STOCKCSV-002 / HIGH / MITIGATED: No direct Item.in_stock write was added; commit routes through the existing adjustment service and creates adjustment lines plus stock movements.
RISK-STOCKCSV-003 / MEDIUM / MITIGATED: FIFO allocation reuses the manual adjustment path, and enabled Woo writeback queues the existing chunked worker after local commit.
RISK-STOCKCSV-004 / HIGH / MITIGATED: Partial stock success can leave the database inconsistent with the submitted physical count. Backend and UI block commit unless every source row is valid or already unchanged; exclusions also block.
RISK-STOCKCSV-005 / HIGH / MITIGATED: Blank location resolves one assignment, an already-matching total, or a zero across unallocated locations; a changed positive multi-location total blocks with an actionable ambiguity error.
RISK-STOCKCSV-006 / HIGH / MITIGATED: Every CSV item, including unchanged rows, is locked before the final stale check and remains locked through commit. Resolved location IDs also detect code/name aliases targeting the same stock row.

=== 11. IMPLEMENTATION AND CHANGE LOG ===
2026-08-08: Added OpenOrderRoutePlan request/response schemas, route-planning service, and POST /api/routes/open-orders/plan.
2026-08-08: Added postal-area sorting, balanced driver groups, missing-address exclusions, four-stop mobile link segmentation, and optional return link.
2026-08-08: Added live route planner UI with default Pongo start, 1–50 drivers, stop cards, warnings, native share/clipboard fallback, and Open Maps links.
2026-08-08: Added MobileCodeScanner to all Inventory views and made both Inventory and Items scan values execute search immediately.
2026-08-08: Added backend/frontend tests and updated README, API, QA, UI, and ADR documentation.
2026-08-09: Committed and pushed 96c3816 to the feature branch and GitHub main, captured Heroku backup b004, paused the worker, deployed Heroku v40, ran the existing Alembic release command, restored the worker, and completed live smoke verification.
2026-08-10: Approved exact stock-by-location CSV override and recorded requirements, safety contract, and implementation plan. No production action taken.
2026-08-10: Implemented Override stock levels, editable current-stock export, exact per-location preview/variance, locked audited batch commit, FIFO follow-up, and optional queued Woo stock sync. Updated UI, tests, and documentation; no deployment.
2026-08-10: Committed and pushed 64fa33c to the protected-workflow feature branch, captured Heroku backup b005, paused the worker, deployed Heroku v42, completed the existing Alembic release command, restored the worker, and passed non-destructive live verification.
2026-08-10: Repaired direct full-export stock override, optional warehouse/location scope, safe blank-location handling, strict all-or-nothing commit, pre-stale locking for unchanged rows, resolved-location duplicate detection, direct Items action, automatic mapping bypass, recovery for old exclusions, and clear no-change completion UX.
2026-08-10: Committed and pushed 462823e to the protected-workflow feature branch, captured Heroku backup b006, paused the worker, deployed Heroku v43, completed the existing Alembic release command, restored the worker, and passed non-destructive live schema/UI verification.

=== 12. TEST, QUALITY, SECURITY, AND PERFORMANCE EVIDENCE ===
Final evidence:
- Focused backend routes: 10 passed; full backend: 495 passed, 10 PostgreSQL-only skipped, one existing Starlette TestClient deprecation warning.
- Focused frontend App suite: 114 passed; full frontend: 7 files and 140 tests passed.
- Frontend production build: passed; existing Vite large-chunk warning remains.
- `git diff --check`: passed after all code and documentation edits.
- Desktop and 390 px mobile browser layouts visually verified; planner controls and Inventory Scan code entry fit without horizontal overflow. Local test browser tab and servers were closed.
- Production Heroku build passed with zero npm audit vulnerabilities; the existing Vite large-chunk advisory remains.
- Heroku release v40 is commit 96c3816 and rollback-eligible. Release migration completed transactionally at schema 20260807_0037.
- Production `/health` returned HTTP 200; anonymous `/api/auth/me` and `/api/routes/open-orders/plan` returned HTTP 401.
- Authenticated production Routes rendered the new planner/default warehouse and completed its read-only request; Inventory exposed exactly one camera scan action; browser console errors were empty.
Security/safety: planning is authenticated through the existing API router, performs no writes, returns no credentials, and calls no map or WooCommerce service. Scanner search performs no stock mutation.
Browser observation: planner layout and controls rendered locally. Data calls failed because the selected local SQLite file lacks existing migrations (`woocommerce_configuration` table and `orders.is_historical_snapshot` column); this is separate from the fresh-database test evidence.
Stock CSV verification: focused backend import suite 15 passed; full backend 498 passed and 10 PostgreSQL-only skipped; full frontend 141 passed; production frontend build passed; `git diff --check` passed. The existing Starlette TestClient deprecation and Vite large-chunk warning remain.
Production stock CSV evidence: Heroku v42 reports commit 64fa33c; `/health` returned 200; anonymous auth and import-schema requests returned 401; PostgreSQL is at Alembic head 20260807_0037; deployed schema version 2026-08-10.1 lists `update_stock`; signed-in UI displayed Override stock levels plus both stock template links with no console errors. No CSV was uploaded and no stock was mutated.
Full-file stock repair verification: 21 focused import tests passed; full backend 504 passed with 10 PostgreSQL-only skips; full frontend 145 passed; production build passed; both independent backend/UI re-reviews found no remaining blocker; `git diff --check` passed. The 1,500-row SQLite regression previewed in 0.236 seconds and committed all rows in 0.925 seconds through one adjustment. Existing Starlette TestClient deprecation and Vite large-chunk warnings remain.
Production full-file repair evidence: Heroku v43 reports commit 462823e; manual PostgreSQL backup b006 completed before deployment; web and worker Basic dynos are up; `/health` returned HTTP 200; anonymous import schema returned HTTP 401; PostgreSQL is at Alembic head 20260807_0037; deployed schema 2026-08-10.2 requires SKU and In stock for `update_stock`; signed-in Items and stock-import screens rendered the direct workflow with no browser console errors. No CSV was uploaded and no inventory changed.

=== 13. DATA MIGRATION, RELEASE, AND ROLLBACK STATE ===
Database migration: none; Heroku's release command confirmed existing head 20260807_0037.
Release: Heroku v43, commit 462823e, 2026-08-10T07:08:52Z.
Recovery: manual PostgreSQL backup b006 completed before v43 (378.00 MB source, 22.59 MB backup). Application rollback is Heroku v42 at 64fa33c. No schema change was included; PostgreSQL remains at 20260807_0037.
PRODUCTION APPROVAL: User explicitly requested “deploy everything” on 2026-08-09.
ACTIVE STOCK CSV RELEASE STATE: Released and verified on production. No real stock-changing smoke import was performed because live inventory mutation was not needed to prove deployment and requires a controlled staff test row.

=== 14. OPERATIONS, OBSERVABILITY, AND SUPPORT READINESS ===
The route endpoint is synchronous and read-only with one order query; no job queue is required for current Pongo scale. Web and worker dynos are each running at one Basic dyno. Production UI and API smoke checks passed without browser console errors. Physical iPhone/Android camera permission and real barcode decoding still require device verification by Pongo staff.
Stock CSV validation and local commit remain synchronous at current Pongo scale and use the existing import-size limits, database transaction, stock mutation lock, adjustment audit, and idempotency controls. When live WooCommerce writeback is enabled, the already-existing chunked stock-sync worker is queued after commit so remote API calls do not hold the import request open.

=== 15. TECHNICAL DEBT AND DEFERRED WORK ===
Deferred: road/traffic optimization, address validation/geocoding, embedded map preview, persisted dispatch plans, named drivers, per-driver acknowledgements, route completion/proof of delivery, and automated SMS/email sharing. Add only after Pongo validates the simple operational workflow and production measurements justify more infrastructure.
Deferred for stock CSV: changing allocated/sellable quantities, creating missing master data, background jobs, and customer-facing stock rollback. Add only with explicit requirements and tested operational rules.
Known unrelated audit debt remains in PRODUCT_AUDIT.txt.

=== 16. CONTINUOUS IMPROVEMENT AND LEARNING ===
Reused the existing scanner component and search flows, the operational order eligibility clause, and platform-native Web Share/clipboard capabilities. For stock CSV, the design deliberately reuses the existing import and stock-adjustment pipelines instead of adding a second stock mutation system, database schema, dependency, or background job.

=== 17. EXACT NEXT ACTIONS AND REQUIRED APPROVALS ===
NEXT: Pongo staff can open Items -> Update stock CSV, upload the complete inventory CSV, and review every exact stock change before confirming.
THEN: Commit only when the file is fully ready; compare the results count and a fresh inventory export after completion. Backup b006 remains the pre-import recovery point.
APPROVAL RECEIVED: The user explicitly requested implementation and production deployment on 2026-08-10. Separate approval remains required for an agent-run real inventory-changing import.

=== 18. COMPLETE APPEND-ONLY DELIVERY HISTORY ===
--- DELIVERY RUN: 2026-08-07T18:54:03Z ---
MODE: Operate
SUMMARY: One-click Google Sheets OAuth implemented, deployed, and verified on Pongo production.
EVIDENCE: Commit 99f390a; Heroku v36; 436 backend tests passed with 7 PostgreSQL-only skips; 107 frontend tests passed; production build and fresh migration 0036 passed; live health 200, anonymous auth 401, authenticated Settings showed Connect Google Account and exact callback URI.
NEXT ACTION: Pongo operator must register the displayed callback in a Google OAuth web client and approve access through the in-app button.

--- DELIVERY RUN: 2026-08-09T08:34:59Z ---
MODE: Operate
SUMMARY: Phone-camera inventory scanning and read-only all-open-order multi-driver Google Maps planning implemented and verified locally; not deployed.
EVIDENCE: 495 backend tests passed with 10 PostgreSQL-only skips; 140 frontend tests passed; production build and diff check passed; desktop and 390px mobile layouts visually verified.
NEXT ACTION: Review, then explicitly authorize commit/push/deployment and production iPhone/Android plus real-order verification.

--- DELIVERY RUN: 2026-08-09T08:51:33Z ---
MODE: Release Readiness and Release
SUMMARY: Inventory camera scanning and open-order multi-driver Google Maps planning deployed to Pongo production as Heroku v40 and verified.
EVIDENCE: Commit 96c3816; backup b004 completed; Alembic release command passed at 20260807_0037; web and worker up; health 200; auth and planner anonymous boundary 401; signed-in Routes and Inventory smoke checks passed with no browser console errors.
NEXT ACTION: Verify physical camera decoding on iPhone/Android and separately address the pre-existing readiness gaps for missing unit costs, alerting, and active stock writeback.

--- DELIVERY RUN: 2026-08-10T06:28:07Z ---
MODE: Build Approved Scope
SUMMARY: Implemented and locally verified exact per-location stock override through the guided item CSV import workspace; not deployed.
EVIDENCE: 15 focused backend import tests, 498 full backend tests with 10 PostgreSQL-only skips, 141 frontend tests, production frontend build, and git diff check passed. Stock writes use locked idempotent adjustment batches with movement audit, FIFO follow-up, stale-preview protection, and queued Woo sync when enabled.
NEXT ACTION: Review the local change, then explicitly approve commit/push/deployment if desired; release should include backup and a small production stock-CSV smoke test.

--- DELIVERY RUN: 2026-08-10T06:31:21Z ---
MODE: Release Readiness and Release
SUMMARY: Production deployment authorized; exact release scope and local verification confirmed, with no schema migration or new dependency.
EVIDENCE: Target is pongo-inventory-os-kannan on Heroku. Current source revision is c94a0ca plus the verified stock CSV, parser, print typography, tests, and documentation changes; git diff check and credential-pattern scan passed.
NEXT ACTION: Commit and publish the release, capture a production PostgreSQL backup and current release baseline, deploy, then run non-destructive live checks.

--- DELIVERY RUN: 2026-08-10T06:37:02Z ---
MODE: Release Readiness and Release
SUMMARY: Audited stock CSV override, CSV parser correction, and larger print typography deployed and verified on Pongo production as Heroku v42.
EVIDENCE: Commit 64fa33c; manual PostgreSQL backup b005 completed; Alembic 20260807_0037 head; web and worker up; health 200; anonymous auth/import schema 401; deployed import schema 2026-08-10.1 includes update_stock; signed-in UI and template links verified with no console errors; no inventory changed.
NEXT ACTION: Pongo staff should preview one controlled physical-count row and commit it only after confirming the displayed before/after quantity.

--- DELIVERY RUN: 2026-08-10T07:05:47Z ---
MODE: Release Readiness and Release
SUMMARY: Direct full-inventory CSV stock replacement repaired and verified for an all-or-nothing production release; not yet deployed.
EVIDENCE: User file is valid with 1,726 rows and 456 blank zero-stock locations; 21 import tests, 504 full backend tests with 10 PostgreSQL-only skips, 145 frontend tests, production build, diff check, and two independent re-reviews passed. Generated 1,500-row stock commit used one adjustment/transaction in 0.925 seconds on local SQLite.
NEXT ACTION: Commit/push the feature branch, capture a new production PostgreSQL backup, deploy to Heroku, and verify live schema/UI without changing stock.

--- DELIVERY RUN: 2026-08-10T07:14:11Z ---
MODE: Release Readiness and Release
SUMMARY: Atomic full-inventory CSV stock replacement deployed and verified on Pongo production as Heroku v43.
EVIDENCE: Commit 462823e; PostgreSQL backup b006 completed; Alembic 20260807_0037 head; web and worker up; health 200; anonymous import schema 401; deployed schema 2026-08-10.2; signed-in Items and stock import workflow verified with no console errors; no inventory changed during deployment checks.
NEXT ACTION: Pongo staff may upload the full CSV from Items -> Update stock CSV, review the all-or-nothing preview, and confirm the exact stock replacement.
