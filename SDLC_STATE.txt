=== 01. STATE METADATA ===
SCHEMA VERSION: 1
PROJECT: pongoOS
REPOSITORY ROOT: /Users/kannankhosla/Desktop/Codex/pongoOS
LAST UPDATED: 2026-08-07T18:54:03Z
EXECUTION MODE: Operate
CURRENT PHASE: Complete
CURRENT RELEASE RECOMMENDATION: DEPLOYED AND VERIFIED FOR PONGO USE

=== 02. EXECUTIVE STATUS IN PLAIN LANGUAGE ===
Pongo Inventory OS is live and its report-to-Google-Sheets credentials are stored encrypted in the Pongo database. The active delivery replaces manual refresh-token entry with a one-click Google OAuth consent flow and then deploys the verified build. Existing production stock authority and WooCommerce writeback configuration remained unchanged and out of scope.

=== 03. PRODUCT VISION, USERS, AND SUCCESS METRICS ===
Primary user: authenticated Pongo staff configuring report sharing. Success means the user enters the Google OAuth application client ID/secret once, clicks Connect Google Account, approves access at Google, returns to Pongo with a connected status, and never handles a refresh token. Existing report publishing must continue to use the encrypted database configuration.

=== 04. REQUIREMENTS AND ACCEPTANCE CRITERIA ===
REQ-GOAUTH-001: Settings shows OAuth client ID, client secret, optional Drive folder, and the exact authorized redirect URI; it does not ask for a refresh token.
REQ-GOAUTH-002: Connect redirects through Google OAuth with Sheets and Drive-file scopes, offline access, consent prompt, and signed expiring state bound to the authenticated actor and callback URI.
REQ-GOAUTH-003: Callback validates state, exchanges the single-use code server-side, encrypts the refresh token, and redirects to a clear success/denied/failure state.
REQ-GOAUTH-004: Secrets and refresh tokens are never returned to the browser or logged.
REQ-GOAUTH-005: Existing manual backend configuration remains compatible; no database migration or hosting-provider dependency is introduced.
REQ-GOAUTH-006: Backend regression tests, focused frontend tests, production build, and migration check pass before deployment; live health and the Settings screen are verified afterward.

=== 05. SCOPE, NON-GOALS, CONSTRAINTS, AND ASSUMPTIONS ===
IN SCOPE: OAuth start/callback backend, encrypted credential persistence, Settings UI, focused tests, reporting/API/operations documentation, deployment and read-only live verification.
NON-GOALS: Google account administration, disconnect/revocation, multi-organization credential isolation, new Google libraries, stock authority/writeback changes, production data changes.
CONSTRAINTS: Authenticated internal staff only; existing master encryption key and database table are reused; Google requires a separately created OAuth web client and exact redirect URI registration.
ASSUMPTION: The authenticated Pongo session remains valid during the short Google consent round trip.

=== 06. SYSTEM, JOURNEY, AND DATA MAP ===
Journey: Settings -> enter/retain OAuth app credentials -> POST OAuth start -> signed Google authorization URL -> Google consent -> authenticated callback -> code exchange -> encrypted refresh-token save -> Settings success state -> existing verified-report publish path.
Data: GoogleReportsConfiguration row id 1 stores encrypted client ID, client secret, refresh token, optional folder ID, actor and timestamp. No plaintext secret is returned to React.
Trust boundaries: browser to authenticated Pongo API; Pongo backend to Google authorization/token endpoints; encrypted database persistence.

=== 07. ARCHITECTURE AND DECISION REGISTER ===
ADR-GOAUTH-001 ACCEPTED: Use OAuth 2.0 authorization-code flow with the existing httpx dependency and Python standard-library HMAC/base64 utilities. No Google SDK or new dependency.
ADR-GOAUTH-002 ACCEPTED: Signed stateless state expires after 10 minutes and binds actor plus redirect URI. Google authorization codes provide one-use exchange semantics.
ADR-GOAUTH-003 ACCEPTED: Keep the existing manual configuration endpoint for compatibility, but remove refresh-token entry from customer navigation.
ADR-GOAUTH-004 ACCEPTED: Use the existing encrypted database configuration so moving away from Heroku does not require redesign.

=== 08. ROADMAP AND ACTIVE DELIVERY PLAN ===
1. Inspect existing Google configuration/report publishing path and current tests. COMPLETED.
2. Implement OAuth start, callback, secure persistence, and redirect status. COMPLETED.
3. Replace manual refresh-token UI with one-click consent and exact redirect URI guidance. COMPLETED.
4. Add backend/frontend regression coverage and update docs. COMPLETED.
5. Run backend/frontend/build/migration/release checks. COMPLETED for available local checks.
6. Commit, push, deploy, and verify live health/UI. IN PROGRESS; explicitly authorized by user on 2026-08-07.

=== 09. WORK ITEM REGISTER ===
GOAUTH-001 / P0 / COMPLETED / Backend: authorization URL, expiring encrypted state, callback exchange and encrypted refresh-token persistence.
GOAUTH-002 / P0 / COMPLETED / Frontend: one-click connection screen with accessible status, exact redirect URI, and no refresh-token input.
GOAUTH-003 / P0 / COMPLETED / QA: OAuth security/route tests, frontend behavior, full regression/build and fresh migration.
GOAUTH-004 / P0 / COMPLETED / Release: commit 99f390a pushed and deployed as Heroku v36; health/auth boundary and authenticated UI passed.

=== 10. RISK, DEPENDENCY, AND BLOCKER REGISTER ===
RISK-001 / HIGH / MITIGATED: OAuth state tampering or callback mix-up. Encrypted authenticated state expires after ten minutes and is bound to actor plus redirect URI; tamper test passes.
RISK-002 / HIGH / MITIGATED: Google may not return a refresh token. Consent/offline access are forced and callback fails closed with actionable UI.
RISK-003 / MEDIUM / MITIGATED FOR DELIVERY: OAuth client redirect URI may not be registered in Google Cloud. Settings displays the exact callback URI; external Google-client setup remains a Pongo operator dependency.
RISK-004 / HIGH / ACCEPTED FOR THIS SCOPED RELEASE: PRODUCT_AUDIT documents unrelated P1 release/stock findings and a non-green historical release gate. This delivery does not enable stock authority/writeback; full relevant regression evidence is required before this deployment.
DEPENDENCY-001: Google OAuth web client must be created by Pongo and configured with the displayed redirect URI before Connect can complete.

=== 11. IMPLEMENTATION AND CHANGE LOG ===
2026-08-07: Delivery initialized at HEAD 99eae99. Existing encrypted Google configuration and report publishing flow retained.
2026-08-07: Added authenticated OAuth start/callback routes, ten-minute encrypted actor/URI-bound state, server-side code exchange, encrypted refresh-token persistence, and safe callback redirects.
2026-08-07: Replaced manual refresh-token UI with Connect/Reconnect Google Account, exact redirect URI guidance, denial/failure/success states, and updated report/configuration copy.
2026-08-07: Updated API, reporting, and production operations documentation. No schema or dependency change.
2026-08-07: Committed 99f390a, pushed the existing branch, and deployed Heroku v36. Release command completed successfully with schema remaining at 20260807_0036.

=== 12. TEST, QUALITY, SECURITY, AND PERFORMANCE EVIDENCE ===
Current delivery evidence at 2026-08-07T18:49Z:
- Focused backend reporting/OAuth: 34 passed, 1 existing Starlette TestClient deprecation warning.
- Full backend SQLite suite: 436 passed, 7 PostgreSQL-only skipped, 1 existing Starlette TestClient deprecation warning.
- Focused Google UI: 2 passed, 88 skipped by filter.
- Full frontend: 6 files and 107 tests passed.
- Production frontend build: passed; existing charts/main chunks remain above Vite's 500 kB advisory threshold.
- Fresh isolated SQLite Alembic upgrade: passed through 20260807_0036. Initial attempt through the venv Alembic executable failed only because its shebang points to an old local path; `python -m alembic` passed.
- `git diff --check`: passed.
Security proof: state integrity/expiry/actor binding implemented; tampered state blocked before exchange; callback code is server-only; secrets remain encrypted/masked; route authentication and SameSite=Lax session behavior retained.

=== 13. DATA MIGRATION, RELEASE, AND ROLLBACK STATE ===
Database migration: none planned; reuse schema 20260807_0036.
Release target: existing Heroku production application at inventory.pongo.ca after tests pass.
Rollback: redeploy previous release/commit 99eae99. OAuth data is additive configuration; previous manual endpoint remains compatible.
PRODUCTION APPROVAL: User explicitly requested “have one click google sign on, and deploy it” on 2026-08-07.

=== 14. OPERATIONS, OBSERVABILITY, AND SUPPORT READINESS ===
Post-deploy evidence: Heroku v36 released commit 99f390a and remains rollback-eligible; release migration succeeded; custom-domain `/health` returned HTTP 200; anonymous `/api/auth/me` returned HTTP 401. The authenticated live Settings page rendered Not connected, Connect Google Account, no refresh-token field, and the exact production callback `https://inventory.pongo.ca/api/reports/google-sheets/oauth/callback`. Google consent was not initiated and no Google account was modified.
Build observations: Heroku build succeeded; existing test-only npm high-severity advisory and Vite large-chunk warnings remain visible and unchanged.
Support: callback success/denied/failed states return the user to Settings with an actionable message.

=== 15. TECHNICAL DEBT AND DEFERRED WORK ===
Deferred: disconnect/revoke action, multi-organization isolation, granular admin RBAC, and provider-independent secrets/KMS strategy. Add only when Pongo expands beyond the current trusted single-organization deployment.
Known unrelated audit debt remains in PRODUCT_AUDIT.txt and is not broadened into this scoped change.

=== 16. CONTINUOUS IMPROVEMENT AND LEARNING ===
Ponytail decision: reuse existing Settings page, encrypted row, httpx, and platform primitives; no OAuth SDK or schema migration.
UI guidance applied: one primary CTA, visible keyboard focus through existing controls, aria-live status, precise error copy, and the exact redirect URI rather than a long onboarding wizard.

=== 17. EXACT NEXT ACTIONS AND REQUIRED APPROVALS ===
NEXT: Pongo operator creates/uses a Google OAuth web client, registers the exact callback shown in Settings, enters client ID/secret, and clicks Connect Google Account.
THEN: approve Google Sheets and Drive-file access in Google's consent screen; Pongo will return to Settings and show Connected.
APPROVAL REQUIRED: Only the user should grant Google account access. No engineering or hosting change is required.
APPROVALS: Production deployment is already explicitly approved. Google consent itself remains a user action because it grants access to the user's Google account.

=== 18. COMPLETE APPEND-ONLY DELIVERY HISTORY ===
--- DELIVERY RUN: 2026-08-07T18:54:03Z ---
MODE: Operate
SUMMARY: One-click Google Sheets OAuth implemented, deployed, and verified on Pongo production.
EVIDENCE: Commit 99f390a; Heroku v36; 436 backend tests passed with 7 PostgreSQL-only skips; 107 frontend tests passed; production build and fresh migration 0036 passed; live health 200, anonymous auth 401, authenticated Settings showed Connect Google Account and exact callback URI.
NEXT ACTION: Pongo operator must register the displayed callback in a Google OAuth web client and approve access through the in-app button.
