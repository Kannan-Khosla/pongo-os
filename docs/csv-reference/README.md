# CSV Reference

Place real Zenventory CSV exports in this folder when they are available.

Future Codex tasks must inspect the actual CSV header before changing import,
export, database mapping, or WooCommerce sync field-mapping logic.

The current canonical inventory column list is based on user-provided
Zenventory export columns and is documented in `docs/CSV_COLUMNS.md`.
